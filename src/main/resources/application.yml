spring:
  application:
    name: stardy-user

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_EXPIRATION}
  refresh-token-expiration: ${JWT_REFRESH_EXPIRATION}
  temporary-code-expiration: ${JWT_TEMPORARY_CODE_EXPIRATION}